# Simvay Collector — backlog

Updated 2026-08-05. **1.0.2 is packaged, validated, and deployed once from the OVA with a clean
first boot** — banner with URL + passphrase, unique hostname `simvay-collector-964538`, and the new
`DHCP assigned from 10.10.76.1` line all confirmed live on clone `SIMVAY-TEST-102` (10.10.76.48).
Deployment Guide v2.3 was validated window-by-window against the real ESXi 8.0U3 host-client wizard
via a live browser walk; `VALIDATE-ON-HOST` markers are stripped. Guide is now 7 pages (the
step-per-window §3 earned it). Structural-humanizer audit: clean, no changes needed.

## 1. Live-walk findings folded into guide v2.3 (all applied)

- Exact quirk text is **"A required disk image was missing."** (past tense, red, top of Ready to
  complete). Guide + troubleshooting row updated verbatim.
- The wizard has an **"Additional settings" window on STANDALONE ESXi 8.x** showing all four OVF
  property categories as collapse rows. The old claim "standalone does not show the properties page"
  is FALSE on 8.0U3 and is corrected in the guide and (pending) HANDOFF §7's browser-first rationale.
- A **"License agreements" step appears in the sidebar until the OVA is parsed**, then disappears
  (no EULA in our OVA). One sentence added so nobody is confused.
- "Power on automatically" comes **pre-checked** on Deployment options; guide says uncheck.
- Upload timing: **~7 minutes** for the 2.6 GB OVA on the local network, with the wizard's own
  do-not-refresh warning noted.

## 2. Remaining before calling 1.0.2 released

1. **Static IP test on the .48 clone — THE fix this cut exists for.** Needs a human to click
   through the cert interstitial once at https://<clone>:8443 (Chrome automation cannot); after
   that one click Claude can drive the console UI, or Ryan just runs the test himself: sign in,
   set a static address, confirm the browser reconnects at the new address and rollback-confirm
   works, then set it back / redeploy.
2. **Live messages check** on a configured clone (was demoed on the v9 master pre-seal — re-check
   post-clone if desired, not strictly required).
3. **OVF-properties-on-standalone question (NEW, from the live walk):** we left all fields blank,
   so whether the host client actually delivers values to the guest (guestinfo.ovfEnv) is untested.
   Next deploy, set Hostname in Additional settings and check
   `journalctl -t s1-collector-firstboot | grep -i ovf` on the clone. If standalone DOES deliver,
   HANDOFF §7's "only vCenter can deliver OVF properties" decision rationale gets revised (the
   browser-first design stays right regardless).
4. Housekeeping: seeds v1–v9 off the datastore (v10 is current), dead 1.0.0/1.0.1 artifacts,
   `C:\Temp` stale exports, tear down test VMs when done. Gold master was destroyed post-export
   (task log 2026-08-04) — next cut rebuilds from seed v10, which carries everything.

## 3. Current release state

| | |
|---|---|
| 1.0.0 | DEAD — bug 11 |
| 1.0.1 | Demo/dev only — static networking inoperative (netdef merge bug) |
| **1.0.2** | **Packaged + validated + one clean OVA deploy.** Seed v10 (`d8c4c435…34c259`) is the matching source-of-record seed (adds the DHCP banner line over v9). Static-IP on-clone test is the last open box |

## 4. Engineering debt (unchanged)

govc/PowerCLI VM creation; input-validator in self-test; firstboot key-comment reorder; pvscsi
decision; netconfig SIGPIPE note. Plus: guide screenshots for §4/§5 can now partially come from the
2026-08-05 session captures (console banner screenshot exists); cert-interstitial screenshot still
needs a human browser.
